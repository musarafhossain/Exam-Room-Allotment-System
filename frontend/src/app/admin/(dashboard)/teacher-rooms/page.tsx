import { TeacherRoomView } from 'sections/teacher-rooms/view';

export const metadata = {
  title: 'Dashboard: Teacher Room List',
};

export default function TeacherRoomsPage() {
  return <TeacherRoomView />;
}
